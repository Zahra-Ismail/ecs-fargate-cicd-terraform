name: Terraform + Build + Deploy (ECS Fargate)

on:
  push:
    branches: ["main"]

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  PROJECT: flask-fargate-demo
  ECR_REPO: ${{ vars.ECR_REPO }}
  TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
  TF_LOCK_TABLE: ${{ vars.TF_LOCK_TABLE }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.6.6

      - name: Terraform Init
        working-directory: terraform
        run: |
          terraform init \
            -backend-config="bucket=${TF_STATE_BUCKET}" \
            -backend-config="region=${AWS_REGION}" \
            -backend-config="dynamodb_table=${TF_LOCK_TABLE}" \
            -backend-config="key=ecs-fargate-demo/terraform.tfstate"

      - name: Terraform Apply (Provision Infra)
        working-directory: terraform
        run: |
          terraform apply -auto-approve \
            -var="aws_region=${AWS_REGION}" \
            -var="project=${PROJECT}" \
            -var="ecr_repo_name=${ECR_REPO}" \
            -var="tf_state_bucket=${TF_STATE_BUCKET}" \
            -var="tf_lock_table=${TF_LOCK_TABLE}"

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t ${ECR_REGISTRY}/${ECR_REPO}:${IMAGE_TAG} .
          docker tag ${ECR_REGISTRY}/${ECR_REPO}:${IMAGE_TAG} ${ECR_REGISTRY}/${ECR_REPO}:latest
          docker push ${ECR_REGISTRY}/${ECR_REPO}:${IMAGE_TAG}
          docker push ${ECR_REGISTRY}/${ECR_REPO}:latest

      # NOTE: The ECS service name below must match what Terraform created.
      # With the terraform-aws-modules/ecs module using services = { app = {...} },
      # the service name often becomes "<PROJECT>-app".
      - name: Force new ECS deployment (pull latest)
        run: |
          aws ecs update-service \
            --cluster ${PROJECT} \
            --service ${PROJECT}-app \
            --force-new-deployment \
            --region ${AWS_REGION}

      - name: Wait for ECS service stability
        run: |
          aws ecs wait services-stable \
            --cluster ${PROJECT} \
            --services ${PROJECT}-app \
            --region ${AWS_REGION}
